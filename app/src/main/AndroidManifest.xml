<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ToolApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToolBoxApp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ToolBoxApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".modules.TextConverterActivity"
            android:theme="@style/Theme.ToolBoxApp" />

        <activity
            android:name=".modules.TextEditorActivity"
            android:theme="@style/Theme.ToolBoxApp" />

        <activity
            android:name=".modules.AudioConverterActivity"
            android:theme="@style/Theme.ToolBoxApp" />

        <activity
            android:name=".modules.VideoToolsActivity"
            android:theme="@style/Theme.ToolBoxApp" />

        <activity
            android:name=".modules.WebCrawlerActivity"
            android:theme="@style/Theme.ToolBoxApp" />

    </application>

</manifest>
